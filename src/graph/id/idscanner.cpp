#include "idscanner.h"


namespace graph {

  namespace id {

    /*
    void IdScanner::Scan(type::gid id, void *data, std::size_t) {
      // byte 0 is the flag. If the flag is set to 0 the record is deleted
      if(((std::uint8_t*)data)[0] == 0x0) {
        this->Reclaim(id);
      }

      if(id > this->m_lastId) {
        this->m_lastId = id;
      }
    }*/
  }
}
